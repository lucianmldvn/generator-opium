'use strict';

var express = require('express');
// var controller = require('./<%= routeName %>.controller');

var router = express.Router();

// router.httpVerb('/subroute', controller.MethodName);

module.exports = router;